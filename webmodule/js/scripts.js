// Empty JS for your own code to be here
/*
$(document).ready(function () {
    $('#sendData').onsubmit(function (ev) {
        ev.preventDefault();

        $('#waiting').html('sending...');

        $.ajax({
            url: '/',
            type: 'post',
            enctype: 'multipart/form-data',
            timeout: 800000,
            data: $('#formValidator').serialize(),
            onsuccess: function (data) {
                console.log(data);
                $('#waiting').html('sent!!!!!');
            },
            onerror: function (data) {
                $('#waiting').html('error!! ' + data);
            }
        })
    });
})
*/
